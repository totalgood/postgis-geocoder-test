version: '2.1'
services:
    postgis:
        build: ./PostGIS
        environment:
            - POSTGRES_PASSWORD=yourpasswordhere
        image: docker.io/znmeb/postgis:build
        ports: 
        - '5439:5432'
